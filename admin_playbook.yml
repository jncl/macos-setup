---
- hosts: all
  connection: local

  vars_files:
    - admin_setup.yml
    - osx-local-defaults.yml

  roles:
    - role: geerlingguy.homebrew
      when: homebrew_cask_apps
      tags: homebrew
    - role: geerlingguy.mas
      when: mas_installed_apps
      tags: mas
    - adminconfig
