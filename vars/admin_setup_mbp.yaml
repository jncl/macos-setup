---
# Disable sudden motion sensor (not useful on SSD/Flash drives)
sudden_motion_sensor: false

computer_hostname: "{{ 'Hedgehog2' if ansible_model is search('MacBookPro6,1') else 'Hedgehog3' if ansible_model is search('iMac18,3') else 'Hedgehog4' if ansible_model is search('iMac') else 'Hedgehog5' }}"

hb_dir: "{{ '/opt/homebrew' if ansible_model is search('iMac21,1') else '/usr/local' }}"
hb_prefix: "{{ hb_dir }}/Homebrew"

# setup variables for MacBook Pro
homebrew_installed_packages:
#  - cdrdao
  - ffmpeg
  - fswatch
  - git
  - luarocks
  - minicom
  - nmap
  - reattach-to-user-namespace
  - ssh-copy-id
  - tmux

homebrew_cask_apps:
  - appcleaner
  - carbon-copy-cloner
  - clipy
  - curseforge
  - google-analytics-opt-out
  - hammerspoon
  - handbrake
  - iterm2
  - karabiner-elements
  - moom
  - mysides
  - name-mangler
  - stats
  - textmate
  - tor-browser
  - vlc

homebrew_cask_apps_local:
  - 1password@7.9.10
#  - mdrp@9.1.1

mas_installed_apps:
  - { id: 425424353, name: "The Unarchiver" }
  - { id: 523620159, name: "CleanMyDrive 2" }
  - { id: 823766827, name: "OneDrive" }

pip_items: [pexpect]

gem_items: []

npm_items: []

dockitems_remove:
  - Launchpad
  - Messages
  - Mail
  - Maps
  - Photos
  - FaceTime
  - Calendar
  - Contacts
  - Reminders
  - Notes
  - Freeform
  - TV
  - Music
  - Podcasts
  - News
  - Downloads
  - 'App Store'

dockitems_persist:
  - name: Terminal
    path: /System/Applications/Utilities/Terminal.app/

dockutil_homebrew_cask: hpedrorodrigues/tools/dockutil

dockutil_install: true
